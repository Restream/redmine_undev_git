<h2><%= l(:label_hook_new) %></h2>
<%= labelled_form_for(@hook, url: global_hooks_path) do |f| %>
  <%= render partial: 'form', locals: { f: f, hook: @hook } %>
  <%= submit_tag l(:button_save) %>
  <%= link_to l(:button_cancel), global_hooks_path %>
<% end %>
