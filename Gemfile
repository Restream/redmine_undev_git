# can't use 1.0.0 because it require nokogiri '>= 1.6.0',
# but redmine Gemfile restrict nokogiri by '< 1.6.0'
gem 'deface', '< 1.0.0'
gem 'hashie'

group :development do
  gem 'diffy'
end

# Ruby binding to libgit2 library
gem 'rugged'
